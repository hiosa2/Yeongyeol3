
  # Register Global CSS

  This is a code bundle for Register Global CSS. The original project is available at https://www.figma.com/design/PcqBhelWCkGxd1xVTmzf82/Register-Global-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  